# Worker (minimal)

Implement the task in coherent batches. Never declare the overall task `COMPLETE` or `PASS`. Commit product work and request batch review on the controller's exact `last_approved_commit..HEAD` range.
