# Kanban board task

Implement the requirements in `proposal.md`.

That document is the product specification. Follow it for columns, cards, board behavior, and UI.

Additional constraints:

- preserve a small demo scope
- document how to run the app and what to click
- do not add accounts, collaboration, or a separate backend
